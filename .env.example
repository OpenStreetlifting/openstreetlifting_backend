# ==============================================================================
# OpenStreetLifting Backend - Development Environment Configuration
# ==============================================================================
# Copy this file to .env and adjust values as needed for your local setup.
#
# Usage:
#   - Local development (running on host): Use DB_HOST=localhost
#   - Docker development: docker-compose.yaml overrides DB_HOST to 'postgres'
# ==============================================================================

# ------------------------------------------------------------------------------
# Database Configuration
# ------------------------------------------------------------------------------
# PostgreSQL connection settings
DB_USER=appuser
DB_PASSWORD=apppassword
DB_NAME=appdb

# For local development (running backend on host machine)
DB_HOST=localhost
DB_PORT=5432

# Full database connection string (auto-constructed in docker-compose)
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
DATABASE_URL=postgresql://appuser:apppassword@localhost:5432/appdb

# ------------------------------------------------------------------------------
# Server Configuration
# ------------------------------------------------------------------------------
# HOST: Network interface to bind to
#   - 127.0.0.1: localhost only (default for local dev)
#   - 0.0.0.0: all interfaces (required for Docker containers)
HOST=127.0.0.1

# PORT: Application listening port (internal container port in Docker)
PORT=8080

# ------------------------------------------------------------------------------
# Authentication
# ------------------------------------------------------------------------------
# API_KEYS: Comma-separated list of valid API keys for request authentication
# Leave empty or remove to disable API key authentication
API_KEYS=dev-key-123,dev-key-456
